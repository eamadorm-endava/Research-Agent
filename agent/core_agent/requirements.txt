# Runtime dependencies for deploying this ADK agent (core_agent) to Vertex AI Agent Engine.
# The ADK CLI can use this file by default when it lives in the agent directory.
#
# Notes:
# - For Gemini Enterprise + Drive tools, you need the Google API client libraries.
# - pydantic + pydantic-settings are used by core_agent/config.py at runtime.

google-cloud-aiplatform[adk,agent-engines]>=1.138.0

# Runtime config
pydantic>=2.12.5
pydantic-settings>=2.13.1

# Model Armor client (only required if you use model_armor.py directly)
# google-cloud-modelarmor>=0.4.0

# Google Drive connector deps
google-api-python-client>=2.0.0
google-auth>=2.0.0
google-auth-httplib2>=0.1.0

# Local dev OAuth fallback (optional)
google-auth-oauthlib>=1.0.0

# File extraction / generation
pypdf>=4.0.0
reportlab>=4.0.0
