steps:
  # Step 1: Initialize Terraform with Dynamic Backend
  - name: 'hashicorp/terraform:1.12.2'
    id: 'tf-init'
    entrypoint: 'sh'
    dir: 'terraform/google_calendar_mcp_server_resources'
    args:
      - '-c'
      - |
        terraform init \
          -backend-config="bucket=${PROJECT_ID}-terraform-state" \
          -backend-config="prefix=terraform/state/calendar-mcp-server-resources"

  # Step 2: Format Check (Terraform)
  - name: 'hashicorp/terraform:1.12.2'
    id: 'tf-fmt'
    args: ['fmt', '-check']
    dir: 'terraform/google_calendar_mcp_server_resources'

  # Step 3: Format Check (Python)
  - name: 'python:3.12'
    id: 'python-lint'
    entrypoint: 'sh'
    args:
      - '-c'
      - |
        apt-get update && apt-get install -y make
        pip install uv
        SKIP=terraform_fmt make run-precommit

  # Step 4: Python Tests
  - name: 'python:3.12'
    id: 'python-test'
    entrypoint: 'sh'
    args:
      - '-c'
      - |
        apt-get update && apt-get install -y make
        pip install uv
        make run-calendar-tests

  # Step 5: Docker Build
  - name: 'gcr.io/cloud-builders/docker'
    id: 'docker-build'
    args: ['build', '-t', 'calendar-mcp-test:ci', '-f', 'mcp_servers/google_calendar/Dockerfile', '.']

  # Step 6: Validate (Terraform)
  - name: 'hashicorp/terraform:1.12.2'
    id: 'tf-validate'
    args: ['validate']
    dir: 'terraform/google_calendar_mcp_server_resources'

  # Step 7: Plan (Terraform)
  - name: 'hashicorp/terraform:1.12.2'
    id: 'tf-plan'
    args: ['plan', '-var=mcp_server_cloud_run_image_tag=ci', '-out=tfplan']
    dir: 'terraform/google_calendar_mcp_server_resources'

serviceAccount: 'projects/${PROJECT_ID}/serviceAccounts/${_SA_NAME}@${PROJECT_ID}.iam.gserviceaccount.com'

options:
  logging: CLOUD_LOGGING_ONLY
